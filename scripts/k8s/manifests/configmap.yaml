apiVersion: v1
kind: ConfigMap
metadata:
  name: littlebaby-config
  labels:
    app: littlebaby
data:
  littlebaby.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.littlebaby/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "LittleBaby Assistant",
            "workspace": "~/.littlebaby/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # LittleBaby Assistant

    You are a helpful AI assistant running in Kubernetes.
